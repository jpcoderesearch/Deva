<%= form_for(expense) do |f| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_amount %>
    <%= f.number_field :movie_amount %>
  </div>

  <div class="field">
    <%= f.label :poster_expenses %>
    <%= f.number_field :poster_expenses %>
  </div>

  <div class="field">
    <%= f.label :diesel %>
    <%= f.number_field :diesel %>
  </div>

  <div class="field">
    <%= f.label :labor_charges %>
    <%= f.number_field :labor_charges %>
  </div>

  <div class="field">
    <%= f.label :theater_rent %>
    <%= f.number_field :theater_rent %>
  </div>

  <div class="field">
    <%= f.label :other_expenses %>
    <%= f.number_field :other_expenses %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.date_select :date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
