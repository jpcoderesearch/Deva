<%= form_for(income) do |f| %>
  <% if income.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(income.errors.count, "error") %> prohibited this income from being saved:</h2>

      <ul>
      <% income.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :bike_stand %>
    <%= f.number_field :bike_stand %>
  </div>

  <div class="field">
    <%= f.label :bakery %>
    <%= f.number_field :bakery %>
  </div>

  <div class="field">
    <%= f.label :ticket_amount %>
    <%= f.number_field :ticket_amount %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
